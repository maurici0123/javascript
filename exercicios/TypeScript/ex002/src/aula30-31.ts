export class Pessoa {
    nome: string
    altura: number
    constructor(nome: string, altura: number) {
        this.nome = nome,
            this.altura = altura
    }
}

const coisas: string[] = ['escada', 'corda', 'regua', 'furadeira']

export default { coisas }